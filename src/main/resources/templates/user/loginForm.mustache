<!DOCTYPE html>
<html lang="en">

<head>
    <title>에브리타임</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet" href="/css/common.css">

    <style>
        * {
            padding: 0px;
            margin: 0px;
        }

        .bottom {
            display: flex;
            justify-content: space-between;
        }

        .m_container {
            width: 380px;
            position: absolute;
            left: 40%;
            top: 30%;
        }

        .logo {
            margin-bottom: 15px;
            height: 60px;
        }

        .input {
            margin-bottom: 5px;
            padding: 5px 10px;
            border: 1px solid #d6d6d6;
            background-color: #fff;
        }

        .text {
            border: 0;
            width: 100%;
            height: 28px;
            line-height: 28px;
            font-size: 16px;
            outline: none;
        }

        .btn_login {
            display: block;
            height: 40px;
            width: 100%;
            outline: none;
            margin-bottom: 15px;
            color: #fff;
            font-size: 16px;
            background-color: #c62917;
        }

        .join {
            padding-top: 30px;
            height: 20px;
            text-align: center;
        }

        .join span {
            margin-right: 5px;
            color: #a6a6a6;
            font-size: 16px;
        }

        .join a {
            color: #c62917;
            font-size: 16px;
            font-weight: bold;
        }

        .login_footer {
            position: fixed;
            bottom: 0;
            width: 100%;
            border-top: 1px solid #e3e3e3;
            text-align: center;
            background-color: #f8f8f8;
            display: flex;
            justify-content: center;
        }

        .login_footer div {
            padding: 20px 5px;
        }
    </style>
</head>

<body>

    <div class="container m_container mt-3">
        <h1 class="logo">
            <a href="#">
                <img src="/images/index.login.logo.png" />
            </a>
        </h1>

        <form action="#">
            <div class="mb-3 mt-3 input">
                <input type="text" class="text" id="username" placeholder="아이디" name="username">
            </div>
            <div class="mb-3 input">
                <input type="password" class="text" id="password" placeholder="비밀번호" name="password">
            </div>

            <button type="submit" class="btn_login">로그인</button>

            <div class="form-check mb-3 bottom">
                <label class="form-check-label">
                    <input class="form-check-input" type="checkbox" name="remember"> 로그인 유지</label>
                <div>
                    <a href="#">아이디/비밀번호 찾기</a>
                </div>
            </div>

            <div class="join">
                <span>에브리타임에 처음이신가요?</span>
                <a href="#">회원가입</a>
            </div>
        </form>
    </div>

    <div class="login_footer">
        <div><a href="#">이용약관</a></div>
        <div><a href="#">개인정보처리방침</a></div>
        <div><a href="#">문의하기</a></div>
        <div><a href="#">ⓒ에브리타임</a></div>
    </div>

</body>

</html>